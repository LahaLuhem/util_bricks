abstract class ConstDurations {
  static const tripleDefaultAnimationDuration = Duration(milliseconds: 1200);
  static const doubleDefaultAnimationDuration = Duration(milliseconds: 800);
  static const oneAndHalfDefaultAnimationDuration = Duration(milliseconds: 600);
  static const defaultAnimationDuration = Duration(milliseconds: 400);
  static const halfDefaultAnimationDuration = Duration(milliseconds: 200);
  static const quarterDefaultAnimationDuration = Duration(milliseconds: 100);
  static const Duration zero = Duration.zero;
}
