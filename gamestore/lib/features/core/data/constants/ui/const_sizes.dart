import 'package:flutter/painting.dart';

/// App sizes
abstract class ConstSizes {
  /// Borders
  static const genericCurvatureBorderRadius = BorderRadius.all(Radius.circular(24));

  /// Elevation
  static const defaultElevation = 6.0;
}
